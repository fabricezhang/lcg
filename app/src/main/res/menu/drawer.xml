<?xml version="1.0" encoding="utf-8"?>

<menu xmlns:android="http://schemas.android.com/apk/res/android">
    <group>
        <item
                android:id="@+id/navItemAbout"
                android:checked="true"
                android:icon="@drawable/ic_info_24px"
                android:title="@string/about" />
        <item
                android:id="@+id/navItemMobileSecurity"
                android:checked="true"
                android:icon="@drawable/ic_mobile_24dp"
                android:title="@string/mobile_security" />
        <item
                android:id="@+id/navItemFreeChat"
                android:checked="true"
                android:icon="@drawable/ic_invert_colors_black_24dp"
                android:title="@string/free_chat" />
    </group>
</menu>